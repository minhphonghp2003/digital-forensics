export enum SecurityLevel {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL,
}

export enum LogType {
    SYSTEM,
    SECURITY,
    APPLICATION,
    NETWORK,
}

export enum Status {
    CLOSED,
    DELETED,
    ACTIVE,
}
